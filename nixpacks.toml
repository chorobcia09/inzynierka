[phases.setup]
commands = [
    "echo 'Setup phase...'"
]

[phases.install]
commands = [
    "curl -s https://www.php.net/distributions/php-8.2.0.tar.gz | tar xz",
    "sudo apt-get update && sudo apt-get install -y php-cli php-mysql"
]

[phases.build]
commands = [
    "echo 'Build phase... (PHP project has no build)'"
]

[phases.start]
commands = [
    "php -S 0.0.0.0:8080 -t public"
]

[env]
PORT = "8080"
